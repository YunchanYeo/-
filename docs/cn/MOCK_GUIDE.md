# Mock 数据使用指南（中文）

韩文版本：`mock.md`

## 1. 目标

- 在后端未就绪时保证前端可开发、可联调
- 复现稳定测试数据，减少环境依赖

## 2. 规范

- 页面层不要直接读取 `model/**`
- 统一从 `services/**` 访问数据
- 切换 mock/真实 API 时，返回结构保持一致

## 3. 建议流程

- 先以 mock 完成功能交互
- 再切换到真实 API 做端到端验证
- 发现字段差异时，优先在服务层适配

