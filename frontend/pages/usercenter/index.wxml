<view wx:if="{{showPrivacyPopup}}" class="privacy-auth-mask" catchtouchmove="noopPrivacyCatch">
  <view class="privacy-auth-panel">
    <view class="privacy-auth-title">隐私保护提示</view>
    <view class="privacy-auth-desc">为完成登录、订单与客服服务，需使用你的手机号等信息。请先阅读并同意《小程序用户隐私保护指引》。</view>
    <button
      id="usercenter-privacy-agree-btn"
      class="privacy-auth-btn privacy-auth-btn-primary"
      open-type="agreePrivacyAuthorization"
      bindagreeprivacyauthorization="onPrivacyAgreePrivacyAuthorization"
    >
      同意并继续
    </button>
    <button class="privacy-auth-btn privacy-auth-btn-plain" bindtap="onPrivacyDisagree">暂不同意</button>
  </view>
</view>
<view wx:if="{{currAuthStep == 1}}" class="user-center-card">
  <view class="user-center-card__header login-header-tap" bindtap="onWechatPrimaryLogin">
    <t-avatar image="{{userInfo.avatarUrl}}" class="user-center-card__header__avatar">
      <view class="user-center-card__avatar-fallback">用</view>
    </t-avatar>
    <view class="user-center-card__header__name">微信登录</view>
  </view>
  <button class="user-center-phone-oneclick login-btn-reset" open-type="getPhoneNumber" bindgetphonenumber="onGetPhoneNumberLogin">
    <text class="user-center-phone-oneclick__txt">手机号一键登录</text>
  </button>
</view>
<t-user-center-card
  wx:else
  userInfo="{{userInfo}}"
  isPhoneHide="{{true}}"
  name-class="custom-name-class"
  phone-class="custom-phone-class"
  avatar-class="customer-avatar-class"
  currAuthStep="{{currAuthStep}}"
  bind:gotoUserEditPage="gotoUserEditPage"
/>
<view class="content-wrapper">
  <view class="order-group-wrapper">
    <t-order-group orderTagInfos="{{orderTagInfos}}" bind:onClickTop="jumpAllOrder" bind:onClickItem="jumpNav" />
  </view>
  <view wx:if="{{showWxLoginFallback}}" class="cell-box wx-login-fallback-box">
    <view class="wx-login-fallback-tip">若登录异常，可点此重新发起微信登录。</view>
    <t-button theme="primary" variant="outline" block bind:tap="onWechatPrimaryLogin">重新微信登录</t-button>
  </view>
  <view wx:if="{{needsBindPhone}}" class="cell-box wx-bind-phone-box">
    <view class="wx-login-fallback-tip">绑定手机号后将用作展示昵称（无昵称或「微信用户」时）。</view>
    <button class="wx-bind-phone-btn login-btn-reset" open-type="getPhoneNumber" bindgetphonenumber="onBindPhoneNumber">绑定手机号</button>
  </view>
  <view wx:for="{{menuData}}" wx:key="item" class="cell-box">
    <t-cell-group>
      <t-cell
        wx:for="{{item}}"
        wx:for-item="xitem"
        wx:for-index="xindex"
        wx:key="xindex"
        title="{{xitem.title}}"
        arrow="{{!xitem.icon}}"
        note="{{xitem.tit}}"
        data-type="{{xitem.type}}"
        bordered="{{false}}"
        bind:click="onClickCell"
        t-class="t-cell-padding"
        t-class-note="order-group-note"
        t-class-left="order-group__left"
      >
        <t-icon wx:if="{{xitem.icon}}" name="{{xitem.icon}}" size="48rpx" slot="note" />
      </t-cell>
    </t-cell-group>
  </view>
  <view class="cell-box admin-entry">
    <view class="admin-entry-inner">
      <t-button theme="primary" variant="outline" block bind:tap="gotoAdminLogin">管理员登录</t-button>
    </view>
  </view>
</view>
<view class="footer__version" wx:if="{{versionNo !== ''}}">当前版本 {{versionNo}}</view>
<t-popup visible="{{showMakePhone}}" placement="bottom" bind:visible-change="closeMakePhone" data-index="2">
  <view class="popup-content">
    <view class="popup-title border-bottom-1px" wx:if="{{customerServiceInfo.serviceTimeDuration}}">
      服务时间: {{customerServiceInfo.serviceTimeDuration}}
    </view>
    <view class="popup-phone {{showKefu ? 'border-bottom-1px' : ''}}" bind:tap="call">电话客服</view>
    <button class="popup-phone border-bottom-1px online" open-type="contact" wx:if="{{showKefu}}">在线客服</button>
    <view class="popup-close" bind:tap="closeMakePhone">取消</view>
  </view>
</t-popup>
<t-toast id="t-toast" />
